# Validation config for Hydra-based validation script

# Data paths and settings
data:
  image_size: [256, 256]
  cubicasa:
    path: null #data/cubicasa5k
    val: null #data/cubicasa5k/val.txt
  structured3d:
    path: null #data/structure3D/Structured3D_bbox/Structured3D/
    val: null #data/structure3D/val.json
  aria_synthenv:
    path: null #data/aria/SyntheticEnv/rendered_data
    val: null #data/aria/SyntheticEnv/val.txt
  swiss_dwellings:
    path: null #data/SwissDwellings/modified-swiss-dwellings-v2/all_render
    val: null #data/SwissDwellings/modified-swiss-dwellings-v2/val.txt
  zillow:
    path: null #data/zind/rendered_data
    val: null #data/zind/val.txt

# Dataloader
val:
  batch_size: 32
  num_workers: 8

# Model
model:
  checkpoint: "/local/home/xanadon/mmfe/outputs/contrastive/checkpoints/remote/45119404_proj_dim_32_dino_MoGe1_common_180_[0.0,0.0]_[1.0,1.0]_noise_25_[0.2,0.2]_[0.5,1.5]_train_eq_true/45119404_proj_dim_32_dino_MoGe1_common_180_[0.0,0.0]_[1.0,1.0]_noise_25_[0.2,0.2]_[0.5,1.5]_train_eq_true-val_loss=0.0020-epoch=32.ckpt"  # required, absolute or relative path to .ckpt

# Logging / Outputs
logging:
  output_csv: outputs/metrics/validation_metrics.csv

# Transforms
transforms:
  mean: [0.485, 0.456, 0.406]
  std: [0.229, 0.224, 0.225]
  tf_difficulty: "hard" # "easy", "medium", or "hard", "rot_only"

# Debug visualization
viz_debug: false

# Test Time Augmentation
tta: 8  # Number of augmentations (1 = no TTA, >1 = TTA with rotations) 
